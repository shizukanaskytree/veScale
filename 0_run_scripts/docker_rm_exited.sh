# To remove all containers that have exited, you can use the following command:
# To automatically confirm the removal of all stopped containers when running docker container prune, you can use the -f (force) flag. This flag bypasses the confirmation prompt and proceeds with the pruning immediately.
docker container prune -f
